项目采用 Next.js 16 + React 19 的 App Router 架构，入口为 pages 目录下的根布局与首页组件；通过 tsconfig.json 中的 `@/*` 路径别名统一模块引用，eslint.config.mjs 集成 eslint-config-next 的 core-web-vitals 与 TypeScript 规则，postcss.config.mjs 使用 @tailwindcss/postcss v4 处理样式。next.config.ts 保持默认配置，所有子模块（pages 等）共享同一份构建、类型检查与 lint 管线。